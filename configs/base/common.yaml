run:
  seed: 42

model:
  revision: main
  dtype: bfloat16
  trust_remote_code: false
  load_in_4bit: false
  gradient_checkpointing: true
  attn_implementation: sdpa

tokenizer:
  padding_side: left
  truncation_side: right
  use_fast: true

training:
  output_dir: outputs/${run.name}
  bf16: true
  fp16: false
  gradient_checkpointing: true

wandb:
  project: alpha-newton
  entity: null
  run_name: ${run.name}
  tags: []
