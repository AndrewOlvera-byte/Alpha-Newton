from setuptools import find_packages, setup


setup(
    name="alpha-newton",
    version="0.1.0",
    description="Two-phase DAPO and GRPO ablation research code",
    packages=find_packages(),
    python_requires=">=3.10",
)
